# 升级说明

本文档说明 MathModelAgent 可选功能的真实实现状态，避免按 README 的配置开关误判为“已可用”。状态口径：

- **已实现**：代码可实际运行，且有完整逻辑；
- **仅配置壳**：仅有环境变量/配置项，核心逻辑未实现；
- **未实现**：连配置壳都没有（仅计划或 TODO）。

## 可选功能状态表

| 功能 | 配置开关 | 状态 | 说明 |
|------|----------|------|------|
| Web Search（Tavily） | `SEARCH_ENABLED` + `TAVILY_API_KEY` | 仅配置壳 | 仅有配置项，未实现真实联网搜索逻辑 |
| RAG 知识库 | `RAG_ENABLED` / `RAG_DB_PATH` / `RAG_TOP_K` 等 | 仅配置壳 | 仅有 ChromaDB + Rerank 相关配置项，未实现检索逻辑 |
| HIL 人机协作 | `HIL_ENABLED` / `HIL_TIMEOUT` / `HIL_CHECKPOINTS` | 未实现（仅数据模型） | 仅 `ApprovalMessage` 数据模型与配置项存在，工作流无任何审批接入点，功能不可用 |
| Fallback Hand Off | 无（原计划 `FALLBACK_*` 系列） | 未实现 | 连配置壳都没有，仅有 LLM 调用层的基础重试机制 |
| Evaluator + Feedback | 无（原计划 `EVALUATOR_*` 系列） | 未实现 | 仅在 Agent 基类中留有 TODO 注释 |

> 表中的“配置开关”均指 WebUI 后端（`backend/app/config/setting.py`）中的环境变量。开关存在并不代表功能可用。**即：本表全部五项功能目前均不可用（未实现或仅配置壳）。**

## WebUI 现状与推荐路线

WebUI 是历史遗留产品，主要用于早期功能演示。上表中的开关大多只存在于 WebUI 后端配置中，核心逻辑尚未完成；Fallback 与 Evaluator 甚至还没有配置项。

**比赛推荐使用 Codex + SKILLS 路线**：从 `workspaces/<competition>/` 目录启动 Codex 并调用 `$1start-mathmodel`，其完整性门禁、人工检查点与验收流程为当前主力实现，详见 [Codex CUMCM 使用指南](docs/md/Codex-CUMCM使用指南.md)。

## WebUI 后端架构级遗留（本轮未动）

以下为 backend 的架构级深层遗留，本轮明确不修复，仅如实记录：

- **本地代码解释器无沙箱隔离**：`backend/app/tools/local_interpreter.py` 在本机以任意代码执行方式运行 LLM 生成的代码（可读写全盘、访问凭据），且执行循环同步阻塞整个 asyncio 事件循环——死循环代码可永久挂死单 worker 后端；
- **断点续跑缺失**：结果仅在流程结束一次性落盘，进程中断/崩溃即全量重来；
- **5 小时硬超时**：`backend/app/routers/modeling_router.py` 以 `asyncio.wait_for(timeout=3600*5)` 无差别掐断长任务。

另有审计已发现但同样未动的安全与数据流缺陷（文件路由路径遍历、端点无鉴权、Coder→Writer 数值断链、`llm.py` 无限重试），均不在本轮修复范围。**结论：WebUI 仅作离线单人教学参考，不建议用于正式比赛。**

## 本地验证边界与 CI

- **本地无法验证的项**：本机无 bash、无 PyYAML，`6verity/scripts/writing_check.sh` 的 `bash -n` 语法门禁与 `.github/workflows/ci.yml` 的 YAML 机器解析**未在本地验证**；正式验证由 GitHub Actions 承担——ci.yml 的 `bash-syntax` 作业在 Ubuntu runner 上执行 `bash -n writing_check.sh`，全部作业由 Actions 实际运行并报告结果；
- **本地已完成的验证**：Python 部分已 ast 校验——全部 skill Python 脚本（47 个）与 backend Python 文件（64 个）解析通过；
- **CI 结构**（`.github/workflows/ci.yml`）：`skills-tests`（skill 单元测试）、`backend-lint`（ruff + `compileall`）、`bash-syntax`（`bash -n writing_check.sh`）。首次推送后以 Actions 的实际执行为准。

## 与 Codex 路线的对应关系

- **Web Search（Tavily）** → Codex 路线由 `$mathmodel-literature-research` 的 OpenAlex 可审计证据包承担检索与核验；Tavily 未实现。
- **RAG 知识库** → Codex 路线暂无本地向量检索，建模知识来自各阶段 skill 的内置 `_references/`，外部事实检索走文献证据包。
- **HIL 人机协作** → Codex 路线由 `$1start-mathmodel` 的七个机器强制人工检查点（intake、literature（按需）、contract、model、results、paper、submission）承担；WebUI 审批交互未完成。
- **Fallback Hand Off / Evaluator + Feedback** → Codex 路线暂无等效自动机制，需队员人工兜底。
